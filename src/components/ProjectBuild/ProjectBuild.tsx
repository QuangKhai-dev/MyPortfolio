import React from "react";

type Props = {};

const ProjectBuild = (props: Props) => {
  return (
    <section>
      <div className="container">
        <h2 className="title-section flex items-end text-3xl max-w-max">
          Some Project I&apos;ve Built
        </h2>
      </div>
    </section>
  );
};

export default ProjectBuild;
